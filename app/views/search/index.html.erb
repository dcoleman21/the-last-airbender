<h2>There are <%= @members.count %> members in the <%= @nation %></h2>

<% @members.each do |member| %>
<section class='member'>
    <h4 class='name'><%= member.name %></h4>
    <div class='photo'><%= image_tag("#{member.photo}") %></div>
    <div class='allies'>
    <h4>Allies</h4>
    <ul>
    <% member.allies.each do |ally|%>
        <li><%= ally %></li>
    <% end %>
    </ul>
    </div>
    <div class='enemies'>
    <h4>Enemies</h4>
    <ul>
    <% member.enemies.each do |enemy|%>
        <li><%= enemy %></li>
    <% end %>
    </ul>
    </div>
    <div class='affiliation'>
    <h4>Affiliation</h4>
    <%=member.affiliation%>
    </div>
</section>
<% end %>
